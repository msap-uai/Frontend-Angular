export const environment = {
  production: true,
  baseUrl: 'https://argentina-programa-spring.herokuapp.com/'
};

//Si usamos el comando ng build –prod (consulte la sección a continuación), se inyectará la baseUrl del archivo environment.prod.ts .