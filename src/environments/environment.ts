

export const environment = {
  production: false,
  baseUrl: 'http://localhost:8080/'
};
//Si usamos los comandos ng build y ng serve en su configuración local, se inyectará la baseURL del archivo environment.ts .


